// Public type surface for the `opencode-ship/core` library.
// The runtime is bundled into `dist/core.js` by esbuild. This
// declaration file is intentionally minimal: it relies on the
// bundled runtime's `.d.ts` companion for tooling that wants
// type-checking. Consumers can import values from the runtime
// without needing type checks because every export is documented
// via the runtime's purpose and naming.
export {};

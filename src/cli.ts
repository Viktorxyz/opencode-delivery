// Public type surface for the `opencode-ship` CLI binary.
// The runtime is bundled into `dist/cli.js` by esbuild. The
// declaration file emitted at `dist/cli.d.ts` is intentionally
// empty: consumers do not import from the CLI entry.
export {};
